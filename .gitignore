# ── Rust / Cargo ──
target/
**/*.rs.bk
/src-tauri/Cargo.lock

# ── Node / npm (if frontend tooling is ever added) ──
node_modules/
dist/
.npm

# ── Tauri ──
src-tauri/target/
src-tauri/gen/schemas/
binaries/ffmpeg*
binaries/ffprobe*

# ── Flatpak ──
.flatpak-builder/
flatpak-build/
flatpak-build-sources/

# ── Deb extraction (local tool installs) ──
histv-deb-extract/

# ── OS junk ──
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ── IDE / editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Claude Code ──
.claude/

# ── Build artefacts & installers ──
*.exe
*.msi
*.dmg
*.deb
*.AppImage
*.nsis
_ffmpeg_download*

# ── Logs & temp ──
*.log
encode_log_*.txt
patchnotes.md
historical-patchnotes.md

# ── Local working docs (not shipped) ──
review-*.md
external-code-review.md
*-implementation-plan.md
releasenotes.md

# ── CI intermediate manifests ──
com.histv.encoder.ci.yml

# ── Flatpak repo export (local only) ──
flatpak-repo/
repo/
