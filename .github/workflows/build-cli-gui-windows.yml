name: Build Windows CLI + GUI

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref to build (branch, tag, or SHA)'
        required: false
        default: 'main'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # 1.93.1
        with:
          toolchain: "1.93.1"
          targets: x86_64-pc-windows-msvc

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "=2.10.1" --locked

      - name: Build GUI
        run: cargo tauri build --target x86_64-pc-windows-msvc

      - name: Build CLI
        working-directory: src-tauri
        run: cargo build --release --bin histv-cli --no-default-features --features cli --target x86_64-pc-windows-msvc

      - name: Collect artefacts
        shell: bash
        run: |
          mkdir -p artefacts
          PORTABLE="$GITHUB_WORKSPACE/src-tauri/target/x86_64-pc-windows-msvc/release/histv.exe"
          if [ -f "$PORTABLE" ]; then
            cp "$PORTABLE" "artefacts/histv-windows.exe"
          fi
          CLI_BIN="$GITHUB_WORKSPACE/src-tauri/target/x86_64-pc-windows-msvc/release/histv-cli.exe"
          if [ -f "$CLI_BIN" ]; then
            cp "$CLI_BIN" "artefacts/histv-cli-windows.exe"
          fi
          ls -lh artefacts/

      - name: Upload artefacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: histv-windows-patch
          path: artefacts/*
          retention-days: 14
