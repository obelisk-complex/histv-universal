name: Build Linux CLI + GUI

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref to build (branch, tag, or SHA)'
        required: false
        default: 'main'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # 1.93.1
        with:
          toolchain: "1.93.1"
          targets: x86_64-unknown-linux-gnu

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "=2.10.1" --locked

      - name: Build GUI
        run: cargo tauri build --target x86_64-unknown-linux-gnu --bundles appimage

      - name: Build CLI
        working-directory: src-tauri
        run: cargo build --release --bin histv-cli --no-default-features --features cli --target x86_64-unknown-linux-gnu

      - name: Collect artefacts
        shell: bash
        run: |
          mkdir -p artefacts
          find "$GITHUB_WORKSPACE" -name "*.AppImage" -exec cp {} artefacts/ \;
          CLI_BIN="$GITHUB_WORKSPACE/src-tauri/target/x86_64-unknown-linux-gnu/release/histv-cli"
          if [ -f "$CLI_BIN" ]; then
            cp "$CLI_BIN" "artefacts/histv-cli-linux"
          fi
          ls -lh artefacts/

      - name: Upload artefacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: histv-linux-patch
          path: artefacts/*
          retention-days: 14
